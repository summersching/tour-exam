# 外語英檢 考照中心 — 使用 / 部署說明

領隊・導遊 外語(英語)歷屆試題逐題完整詳解 + 學習中心 + 錯題本。
**100% 離線、免費、無廣告、不需註冊、不連雲端。**

---

## ✅ 已上線(GitHub Pages)

- **App 網址**:https://summersching.github.io/tour-exam/
- **GitHub 專案**:`summersching/tour-exam`(公開;內容為公開考題詳解,無個資)
- **更新方式**:本資料夾已是 git 專案、remote 已設定好。更新只要在本資料夾執行
  `git add -A && git commit -m "說明" && git push`,手機重開 App 即為新版
  (更新內容後記得把 `sw.js` 的 `ylenpass-v1` 版本號 +1,舊快取會自動汰換)。

---

## 檔案結構

```
英文詳解/
├── index.html              ← App 首頁(從這裡進入)
├── 學習中心.html            ← 單字 / 文法 / 技巧 / 陷阱(自 10 屆自動彙整)
├── 錯題本.html              ← 測驗答錯自動收錄,跨年複習
├── 106~115年_外語英語_詳解.html   ← 10 屆逐題詳解(共 10 檔)
├── manifest.json / sw.js / pwa.js  ← PWA(可安裝、離線)設定
└── icon-*.png / apple-touch-icon.png  ← App 圖示
```

---

## 一、電腦上直接用(最快)

用瀏覽器打開 **`index.html`** 即可。首頁 → 選年份看詳解、進學習中心、開錯題本。

> ⚠️ 直接開檔(file://)時,**錯題本無法跨年份共用紀錄**,且無法「安裝成 App」。
> 要完整體驗(手機安裝 + 跨屆錯題本),請照下面部署到 GitHub Pages。

---

## 二、放到手機當 App(GitHub Pages,免費)

### 步驟(用網頁操作,不用會 git)

1. 到 <https://github.com> 註冊 / 登入(免費)。
2. 右上角 **+ → New repository**,取名例如 `en-exam`,選 **Public**,建立。
3. 進 repo → **Add file → Upload files** → 把 `英文詳解` 資料夾裡**所有檔案**拖進去
   (注意:是拖「裡面的檔案」,讓 `index.html` 在最上層)→ **Commit changes**。
4. **Settings → Pages** → Source 選 **Deploy from a branch** → 分支選 `main`、資料夾 `/ (root)` → **Save**。
5. 等 1~2 分鐘,頁面上方會出現網址,例如
   `https://你的帳號.github.io/en-exam/`。
6. **手機**用瀏覽器開這個網址:
   - **Android(Chrome)**:選單 → 「安裝應用程式 / 加到主畫面」。
   - **iPhone(Safari)**:分享鈕 → 「加入主畫面」。
7. 桌面就出現 App 圖示,點開像原生 App,**之後沒網路也能用**,錯題本也會跨 10 屆一起記。

---

## 三、更新內容後

- 改了某年詳解 / 想重新彙整學習中心:重跑產生器(見 scratchpad 的 `gen_learning_center.py`)。
- 每次更新後,把 **`sw.js` 最上面的 `ylenpass-v1` 版本號 +1**(例 `v2`),
  重新上傳,手機重開一次就會自動換成新版(舊快取自動清掉)。

---

## 四、之後要擴充(導遊 / 其他科目)

App 首頁 `index.html` 最上方的 `APP` 資料就是總目錄:

- 加「導遊」:把 `guide` 那段的 `on:false` 改成 `on:true`,補上 `years` / `file` / `meta`,
  並放進對應的詳解 HTML。
- 加其他科目:在該分類的 `subs` 陣列加一個科目即可。

畫面會自動長出對應的分類頁籤與年份卡,不用改其他地方。
